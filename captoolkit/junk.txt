def get_node(name, node):
    if isinstance(node, h5py.Dataset):
        return node
    else:
        pass

# Read
a = f.visititems(get_node)

name = a.name
data = a[:]

# Save
with h5py.File('ofile.h5', 'w') as fo:
    fo[name] = data
